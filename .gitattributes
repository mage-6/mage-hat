# The binary embeds MANUAL.md and parses it for the command list. A CRLF
# checkout on a Windows CI runner broke that parsing silently, so the text
# files the build reads are pinned to LF.
* text=auto eol=lf
*.png binary
*.jpg binary
*.webp binary
*.woff2 binary
*.ico binary
